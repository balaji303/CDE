import setuptools

setuptools.setup(
    name="CDE",
    author="balaji303",
    description="Install Packages",
    packages=setuptools.find_packages(),
    install_requires = ['wxPython'],
    python_requires='>=3.0',
)
